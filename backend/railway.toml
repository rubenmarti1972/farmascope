[build]
builder = "nixpacks"
buildCommand = "npm install -g pnpm && pnpm install && pnpm run build"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/api/report/status"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
